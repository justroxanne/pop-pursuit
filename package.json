{
  "name": "pop-pursuit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix api\" \"npm run dev --prefix client\"",
    "setup": "npm i && npm --prefix api i && npm --prefix client i && npm run migrate --prefix api",
    "migrate": "npm run migrate --prefix api",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0",
    "dotenv": "^16.3.1"
  }
}
